rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // This is the simplest possible rule to allow your app to read from
    // the email_leads collection.
    match /email_leads/{leadId} {
      allow read: if true;
      allow write: if false; // Secure the collection from client-side writes
    }
  }
}
