rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: This is insecure. It allows anyone to read and write to your database.
    // This should only be used for a brief testing period.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
