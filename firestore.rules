
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read access to the leads collection,
    // but deny all writes. Leads are only created via the backend function.
    match /leads_v2/{leadId} {
      allow read: if true;
      allow write: if false;
    }
  }
}
